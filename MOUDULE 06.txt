1-
SELECT *
FROM EMPLOYEES
WHERE  SALARY <(SELECT SALARY
	        FROM EMPLOYEES
	        WHERE EMPLOYEE_ID = 144
	 	);//employee id jar 144 tar theke kom salary gular khetre.Sub query eita


2-Highest salary k pai?

main query -k pai
subquery - salary

SELECT *
FROM EMPLOYEES
WHERE SALARY = (
SELECT MAX(SALARY)
FROM EMPLOYEES
);

3-
MARKETING DEPT A KARA KARA KAJ KORE.TADER AVG SALARY KOTO?

SELECT AVG(SALARY)
FROM EMPLOYEES
WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID
FROM DEPARTMENTS
WHERE DEPARTMENT_NAME = 'MARKETING');

SELECT * 
FROM EMPLOYEES
WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID
FROM DEPARTMENTS
WHERE DEPARTMENT_NAME = 'MARKETING');

4-
IT DEPARTMENT A KOIJON KAJ KORE?

SELECT COUNT(*) AS MEMBERS
FROM EMPLOYEES
WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID
		       FROM DEPARTMENTS
		       WHERE DEPARTMENT_NAME = 'IT');

5-
PROGRAMMER DER PECHONE MONTHLY KOTO COST

SELECT SUM(SALARY)
FROM EMPLOYEES
WHERE JOB_ID = (SELECT JOB_ID
		FROM JOBS
		WHERE JOB_TITLE = 'PROGRAMMER'
                );

ADVANCE SUBQUERY
(1) SECOND HIGHEST SALARY KOTO?K PAI?

WAY 1-> SALARY DESCENDING,DISTINCT,2ND HIGHEST

SELECT *
FROM EMPLOYEES
WHERE SALARY = (SELECT DISTINCT SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC
LIMIT 1
OFFSET 1
);

WAY 2-> SUBQUERY

SELECT *
FROM EMPLOYEES
WHERE SALARY = (SELECT DISTINCT SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC
LIMIT 1
OFFSET 1
);

(2) KON KON EMPLOYEE TADER MANAGER ER CHEYE BESHI SALARY PAI?


SELECT * 
FROM EMPLOYEES AS EMP
WHERE SALARY > (SELECT SALARY
		FROM EMPLOYEES AS MGR
                WHERE EMP.MANAGER_ID = MGR.EMPLOYEE.ID
                )//EITA RETURN KORBE MANAGER ER SALARY

(3)KON KON EMPLOYEE TADER MANAGER ER MOTO SAME JOB KORE?
SELECT *
FROM EMPLOYEES AS EMP
WHERE JOB_ID = (SELECT JOB_ID
		FROM EMPLOYEES AS MGR
		WHERE EMP.MANAGER_ID = MGR.EMPLOYEE_ID
                );

COMMON TABLE EXPRESSION WITH AS
WITH TEMP AS
(
SELECT * 
FROM EMPLOYEES
LIMIT 5
)
SELECT *
FROM TEMP;